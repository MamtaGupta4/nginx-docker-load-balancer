version: '3'

services:
  backend:
    image: hashicorp/http-echo
    command: ["-text=Hello from $(hostname)"]
    expose:
      - "5678"

  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend